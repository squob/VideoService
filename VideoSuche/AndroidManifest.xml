<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uni.leipzig.rr.videosuche"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="uni.leipzig.rr.videosuche.MainActivity"
            android:label="My Activity :)" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
        	android:name=".GestureActivity"
        	android:label="Videosuche"  >
        	<intent-filter>
            <action android:name="uni.leipzig.rr.videosuche.GESTUREACTIVITY" />
            <category android:name="android.intent.category.DEFAULT" />
        	</intent-filter>
    	</activity>
        <activity android:name="MainSettings" android:launchMode="singleTask" android:label="Settings"></activity>
    </application>

</manifest>
